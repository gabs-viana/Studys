calcFrase(1, "Gabs")

function calcFrase(age, name){
    if(age <= 1){
        console.log("Eu me chamo " + name + " e tenho " + age + " ano de idade.")
    }
    else{
        console.log("Eu me chamo " + name + " e tenho " + age + " anos de idade.")
    }
}